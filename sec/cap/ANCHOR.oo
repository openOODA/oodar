// # sec/cap — Capability Tokens
//
// Logline: The capability token system (Pillar 5). 14 NORTHSTAR core (FsRead, FsWrite, Net, Tcp, Udp, Sys, Process, Env, Time, Rand, Alloc, Thread, Gpu, Ffi) + 11 future-state (Fs, Arena, Sign, Audio, Camera, Usb, Hid, Window, Frame, Bind, CompilerRead) for hardware caps not yet wired up. Token derivation uses getentropy(3) — fail-closed (no LCG fallback) per NORTHSTAR. Plus the cap helpers (seal, attenuate, eq).
//
// Beats:
//   1. caps.h — the OODAR_CAP_* bit-layout and the 14 token kinds.
//   2. caps.c — the getentropy-derived token creation and attestation.
//   3. cap.c — seal, attenuate, eq, plus the seccomp-bpf filter application.
//
// ──▓▓ sec/cap ANCHOR ──▓▓──
// :: Pillar 5 — unforgeable, kernel-derived ::
