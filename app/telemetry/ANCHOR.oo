// # app/telemetry — Counter Export
//
// Logline: The per-process counter export. The event bus that decouples
// this from the producing subsystems lives in core/event/.
//
// Beats:
//   1. metrics.c — the counter export; subscribes to 8 named events via a constructor.
//
// v3.1.0 file move: event.{c,h} moved from app/telemetry/ to
// core/event/ to resolve the sec/-depending-on-app/ boundary
// violation. The app/telemetry/ subdir now only contains metrics.c.
//
// ──▓▓ app/telemetry ANCHOR ──▓▓──
// :: consumers listen to events; producers are in their own domains ::
