#ifndef OODAR_TYPES_Q_H
#define OODAR_TYPES_Q_H
/* v2.3.0 split: request (Q) types — reserved for future request/pair/list
 * types (OoRqPair, OoRqI32Pair, OoRqI32List). Currently empty; the
 * umbrella includes this header so future request-type additions land
 * in a single file. */

#endif
